{% if backlinks.length > 0%}
<div class="backlinks-container">
	<p class="backlinks-title larger">See also:</p>
	<ul>
		{% for link in backlinks %}
		<li>
			<a href="{{ link.url }}">{{ link.title }}</a>
		</li>
		{% endfor %}
	</ul>
</div>
{% endif %}
